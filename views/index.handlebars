<div class="d-flex justify-content-center container">
        <form action="/api/todo" method="POST">
            <textarea name="taskName" class="mt-2 form-control"></textarea>
            <div class="button-col">
                <button type="submit" class="btn btn-primary btn-lg">Add a new task</button>
            </div>
        </form>
</div>

<br><br>

{{!-- <div class="container">
    <div class="row">
    <div class="col-md-6">
        <ul class="burger-list">
            <h1 class="text-center">Available to Devour</h1>
            {{#each burgers}} 
                {{#unless devoured}}
                    {{> burgers/burger-button devoured=false}}
                {{/unless}}
            {{/each}}
        </ul>
    </div>
    <div class="col-md-6">
        <ul class="burger-list">
            <h1 class="text-center">Previously Devoured</h1>
            {{#each burgers}} 
                {{#if devoured}}
                    {{> burgers/burger-button devoured=true}}
                {{/if}}
            {{/each}}
        </ul>
    </div>
</div>
</div> --}}



<script src="js/todo.js"></script>